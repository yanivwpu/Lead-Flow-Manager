/** Dependency-safe canonical duration for the one-time Pro + AI Brain trial. */
export const PRO_AI_TRIAL_DAYS = 14;

export function formatProAiTrialDuration(): string {
  return `${PRO_AI_TRIAL_DAYS}-day`;
}
