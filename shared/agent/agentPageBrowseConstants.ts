/** Default page size for Agent Page browse (initial load + load more). */
export const AGENT_PAGE_BROWSE_PAGE_SIZE = 24;

/** Max published rows loaded into memory for server-side browse filtering. */
export const AGENT_PAGE_BROWSE_FETCH_CAP = 10_000;
