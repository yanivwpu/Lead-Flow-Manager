/** Canonical in-app paths for transactional email CTAs. Keep in sync with AppLayout routes. */
export const APP_INBOX_PATH = "/app/inbox" as const;
export const APP_PROSPECT_AI_PATH = "/app/prospect-ai" as const;
export const APP_INTEGRATIONS_PATH = "/app/integrations" as const;
export const APP_TEMPLATES_PATH = "/app/templates" as const;
export const APP_SETTINGS_PATH = "/app/settings" as const;
/** Public pricing page (authenticated Stripe / Shopify checkout lives here — not /app/pricing). */
export const APP_PRICING_PATH = "/pricing" as const;
